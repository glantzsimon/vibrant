@using K9.Base.WebApplication.Helpers
@using K9.WebApplication.Helpers
@model Order

@for (int i = 0; i < Model.ProductPacks.Count; i++)
{
    @Html.HiddenFor(e => Model.ProductPacks[i].Id)
    @Html.HiddenFor(e => Model.ProductPacks[i].ProductPack.Id)
    @Html.HiddenFor(e => Model.ProductPacks[i].ProductPack.Name)

    <div class="row">
        <div class="col-sm-4">
            @Html.BootstrapDisplayFor(e => Model.ProductPacks[i].ProductPack.Name)
        </div>
        <div class="col-sm-4">
            @Html.BootstrapDisplayFor(e => Model.Products[i].PriceTier)
        </div>
        <div class="col-sm-4">
            @Html.BootstrapEditorFor(e => Model.ProductPacks[i].Amount)
        </div>
    </div>
}

