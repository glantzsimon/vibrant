@using K9.Base.WebApplication.Helpers
@using K9.WebApplication.Helpers
@model User

@if (Model.Id > 0)
{
    @Html.BootstrapActionLinkButton(Dictionary.EditProtocols, "EditProtocols", "Users", new { id = Model.Id }, "", EButtonClass.Success)
}